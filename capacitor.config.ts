import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bo.edu.proyecto.orientacion',
  appName: 'sistema-orientacion-aeroportuaria',
  webDir: 'www'
};

export default config;
